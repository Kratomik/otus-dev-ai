Для того чтобы приложение запустить локально требуется:
- Удалить папку с модулями:
rm -rf node_modules

- Установить зависимости:
npm install

- Запустить приложение из папки frontend:
npm run dev

- Запустить тесты из папки frontend:
npm test

