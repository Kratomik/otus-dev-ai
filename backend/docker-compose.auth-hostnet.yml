# Устарело: host network для auth включён в docker-compose.yml по умолчанию.
# Файл оставлен для совместимости: docker compose -f docker-compose.yml -f docker-compose.auth-hostnet.yml …
version: "3.8"

services:
  auth: {}
